<%namespace name="components" file="test_components.mako" />
${components.header(title=title_prefix, projects=projects, current=current, excluded_tests=excluded_tests)}
 ${welcome}
${components.footer()}
